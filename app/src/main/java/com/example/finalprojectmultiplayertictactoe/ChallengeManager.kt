package com.example.finalprojectmultiplayertictactoe

// hanterar förfrågningar av spel utmaningar
class ChallengeManager{

    // metod för att skicka en utmaning till en annan spelare
    fun sendChallenge(player: Player): Boolean{

        return
    }

    // metod för att hantera förfrågningar av utmaningar
    fun reciveChallenge(player: Player): Boolean{

        return
    }

    // metod för att acceptera en utmaning
    fun acceptChallenge(player: Player): Boolean{

        return
    }

    // metod för att neka en utmaning
    fun declineChallenge(player: Player){

    }

}
